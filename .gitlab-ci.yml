stages:
  - build
  - test
  - clean
build:
  stage: build
  script:
    - bash build_gtest.sh
  artifacts:
    paths:
      - ./

test:
  stage: test
  script:
    - make test
    - gtest.exe --gtest_output=\"xml:report.xml\"
  artifacts:
    when: always
    reports:
      junit: report.xml
  

clean:
  stage: clean
  script:
    - make clean
